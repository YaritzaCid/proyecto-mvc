<h1>Listado de afiliados</h1>

<ul>
  {{#each affiliates}}
    <li>
      {{this.firstName}} {{this.lastName}} - {{this.membershipType}}
    </li>
  {{/each}}
</ul>

<h1>Listado de afiliados</h1>

<a href="/affiliates/create">
  Crear afiliado
</a>


<!-- Id por afiliado -->
<ul>
  {{#each affiliates}}
    <li>

      {{this.firstName}} {{this.lastName}}

      <a href="/affiliates/{{this.id}}">
        Ver detalle
      </a>

    </li>
  {{/each}}
</ul>